{
  "name": "social-connect",
  "version": "1.0.0",
  "description": "Social Connect - Full Stack App",
  "scripts": {
    "start": "concurrently --names \"FRONTEND,BACKEND\" --prefix-colors \"cyan,magenta\" \"npm run dev --prefix frontend\" \"node backend/server.js\"",
    "frontend": "npm run dev --prefix frontend",
    "backend": "node backend/server.js"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
